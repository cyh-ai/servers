# 更换sit环境使用-Dspring.profiles.active=sit
spring:
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        new_generator_mappings: false
        format_sql: true
  datasource:
    #    url: jdbc:mysql://localhost:3306/server?serverTimezone=UTC&useSSL=false&autoReconnect=true&tinyInt1isBit=false&useUnicode=true&characterEncoding=utf8
    url: jdbc:mysql://localhost:3306/mybatis?serverTimezone=UTC&useSSL=false&autoReconnect=true&tinyInt1isBit=false&useUnicode=true&characterEncoding=utf8
    username: root
    password: admin
  redis:
    #ip地址
    host: 127.0.0.1
    #端口号
    port: 6379
    #密码
    password: 123456
    #超时时间
    timeout: 130000
    lettuce:
      pool:
        #最大阻塞等待时间，负数表示没有限制
        max-wait: -1
        # 连接池中的最大空闲连接
        max-idle: 8
        # 连接池中的最小空闲连接
        min-idle: 0
        # 连接池中最大连接数，负数表示没有限制
        max-active: 20
server:
  port: 8082

swagger:
  show: true
#加密
#aesDecode:
#  doDecode: 0
#  excludeuri: /**/getIndexRedirectUrl,/**/inn/**  对应方法的路径
#  excludeMethod: handleException,uiConfiguration,swaggerResources    方法名
#  unDecodeMethod: handWriting,checkSalesPlan,queryOrder,buttonActionLink,sxtbOrder
#  doSkipDecode: 1
#  headerSkipVal: tiaoguoba

aesDecode:
  doDecode: 0
  excludeuri:
  excludeMethod:
  unDecodeMethod:
  doSkipDecode: 1
  headerSkipVal: tiaoguoba




